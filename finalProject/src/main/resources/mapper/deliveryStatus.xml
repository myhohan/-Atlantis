<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kh.finalproject.mapper.DeliveryStatusMapper">

    <sql id="searchCondition">
        <if test="status != null and status != '' and status != 'all'">
            AND TRANSPORT_STATUS = #{status}
        </if>
        
        <if test="query != null and query != ''">
            AND (
                POST_NO LIKE '%' || #{query} || '%'
                OR SENDER_NAME LIKE '%' || #{query} || '%'
                OR RECEPIENT_NAME LIKE '%' || #{query} || '%'
            )
        </if>
    </sql>

    <select id="getListCount" resultType="_int">
        SELECT COUNT(*) 
        FROM POSTAL_ITEM
        WHERE MEMBER_DEL_FL = 'N'
        <include refid="searchCondition"/> </select>

  <select id="selectDeliveryList" resultType="com.kh.finalproject.dto.DeliveryStatus">
    SELECT 
        POST_NO,
        SENDER_NAME,
        RECEPIENT_NAME,
        RECEPIENT_ADDRESS, 
        TRANSPORT_STATUS,
        TO_CHAR(REG_DATE, 'YYYY-MM-DD') AS REG_DATE 
    FROM POSTAL_ITEM
    WHERE MEMBER_DEL_FL = 'N'
    <include refid="searchCondition"/> 
    ORDER BY REG_DATE DESC
    
    OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY
</select>

    </mapper>