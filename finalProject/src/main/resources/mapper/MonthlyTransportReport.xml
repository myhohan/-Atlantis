<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kh.finalproject.mapper.MonthlyTransportReportMapper">

	<select id="totalDelivery" resultType="com.kh.finalproject.dto.CompanyManagement">
		SELECT COUNT(POST_NO) AS totaldeliveryNumber
		FROM POSTAL_ITEM
		WHERE AUTHORITY = 2
		AND MEMBER_DEL_FL = 'N'
	</select>
	
	<select id="activePersonnel" resultType="com.kh.finalproject.dto.CompanyManagement">
		SELECT COUNT(*) 
		FROM PERSONNEL_NAME
	</select>

	<select id="monthlyProfit" resultType="com.kh.finalproject.dto.CompanyManagement">
		SELECT TOTAL_MONTHLY_PROFIT as monthlyProfit
		FROM MONTHLY_PROFIT
		</select>

	<select id="customerSatisfaction" resultType="com.kh.finalproject.dto.CompanyManagement">
		SELECT TOTAL_CUSTOMER_SATISFACTION as customerEvaluation
		FROM CUSTOMER_SATISFACTION
		WHERE TOTAL_CUSTOMER_SATISFACTION IS NOT NULL
	</select>
	
	<select id="recentDeliveryActivity" resultType="com.kh.finalproject.dto.CompanyManagement">
		SELECT POST_NO as postNo, 
			   RECENT_ACTIVITY as recentDeliveryActivity
		FROM PERSONNEL
		</select>
	
	<select id="personnelManagement" resultType="com.kh.finalproject.dto.CompanyManagement">
		SELECT PERSONNEL_NAME AS personnelName, 
			   PERSONNEL_ID AS personnelID, 
			   PERSONNEL_TASKS,  
			   CUSTOMER_EVALUATION AS customerEvaluation, 
			   STATUS AS Status
		FROM PERSONNEL
	</select>
	
	<select id="currentStatus" resultType="com.kh.finalproject.dto.CompanyManagement">
		SELECT POST_NO AS postNo, 
			   PERSONNEL_NAME AS personnelName, 
			   CURRENT_ACTIVITY AS Status
		FROM DELIVERY_STATUS
	</select>
	</mapper>